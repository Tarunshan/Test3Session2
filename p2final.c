#include<stdio.h>
typedef struct fraction
{
  int num,den;
}fraction;
fraction input_fraction()
{
  int a;
  printf("Enter numerator and denominator value ");
  scanf("%d%d",&a.num,&a.den);
  return a;
}

fraction largest(fraction f1,fraction f2,frcation f3)
{
  
}